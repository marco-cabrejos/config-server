server:
  port: 8099
  
eureka:
  instance:
    hostname: marcab-spring-registry-server.azurewebsites.net
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
