server:
  port: 8095
  
spring:
  application:
    name: micro-withdrawal
  data:
    mongodb:
      uri: mongodb://localhost:27017/withdrawal

kafka:
  server:
    hostname: localhost
    port: 29092

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/
